import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Aplicativo_07_-_Mensagem',
  webDir: 'www'
};

export default config;
